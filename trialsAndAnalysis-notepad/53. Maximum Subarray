class Solution {
    public int maxSubArray(int[] nums) {
        int[] arr = new int[nums.length];
        for(int i = 0; i < nums.length; i++){
            arr[i] = nums[i];
        }

        int max_sum = Integer.MIN_VALUE;
        for(int k = 1; k < nums.length; k++){
            arr[k] += arr[k-1];
        }

        int ignore_val = nums[0];
        for(int j = 1; j < nums.length; j++){
            for(int l = j; l < nums.length; l++){
                arr[l]-=ignore_val;
                max_sum = Math.max(max_sum, arr[l]);
            }
            ignore_val += nums[j];
        }

    return max_sum;
    }
}

--------------------------------------------------------

class Solution {
    public int maxSubArray(int[] nums) {
        int[] arr = new int[nums.length];
        for(int i = 0; i < nums.length; i++){
            arr[i] = nums[i];
        }

        int max_sum = Integer.MIN_VALUE;
        for(int k = 1; k < nums.length; k++){
            arr[k] += arr[k-1];
        }

        int ignore_val = nums[0];
        for(int j = 1; j < nums.length; j++){
            for(int l = j; l < nums.length; l++){
                arr[l]-=ignore_val;
                max_sum = Math.max(max_sum, arr[l]);
            }
            ignore_val = nums[j];
        }

    return max_sum;
    }
}


-----------------------------------------------------------

class Solution {
    public int maxSubArray(int[] nums) {
        int[] arr = new int[nums.length];

        int max_sum = Integer.MIN_VALUE;

        for(int i = 0; i < nums.length; i++){
            arr[i] = nums[i];
            max_sum += nums[i];
        }

        for(int k = 1; k < nums.length; k++){
            arr[k] += arr[k-1];
        }

        int ignore_val = nums[0];
        for(int j = 1; j < nums.length; j++){
            for(int l = j; l < nums.length; l++){
                arr[l]-=ignore_val;
                max_sum = Math.max(max_sum, arr[l]);
            }
            ignore_val = nums[j];
        }

    return max_sum;
    }
}

--------------------------------------------------------- Only passed few test cases

class Solution {
    public int maxSubArray(int[] nums) {
        int[] arr = new int[nums.length];

        int max_sum = 0; //the issue in the line is reported by ai

        for(int i = 0; i < nums.length; i++){
            arr[i] = nums[i];
            max_sum += nums[i];
        }

        for(int k = 1; k < nums.length; k++){
            arr[k] += arr[k-1];
        }

        int ignore_val = nums[0];
        for(int j = 1; j < nums.length; j++){
            for(int l = j; l < nums.length; l++){
                arr[l]-=ignore_val;
                max_sum = Math.max(max_sum, arr[l]);
            }
            ignore_val = nums[j];
        }

    return max_sum;
    }
}

--------------------------------------------------------- Time Limit Exceeded for the below approach

class Solution {
    public int maxSubArray(int[] nums) {
        int[] arr = new int[nums.length];

        int max_sum = nums[0];

        for(int i = 0; i < nums.length; i++){
            arr[i] = nums[i];
        }

        for(int k = 1; k < nums.length; k++){
            arr[k] += arr[k-1];
            max_sum = Math.max(max_sum, arr[k]);
        }

        int ignore_val = nums[0];
        for(int j = 1; j < nums.length; j++){
            for(int l = j; l < nums.length; l++){
                arr[l]-=ignore_val;
                max_sum = Math.max(max_sum, arr[l]);
            }
            ignore_val = nums[j];
        }

    return max_sum;
    }
}

----------------------------------------------------------------------- //flawed logic, 
/*
consider example [5,1,9,-10] and space is 2 then: 
1. total - (5 + 1)
2. total - (9 - 10)

but you would miss : total - (1 + 9)

*/ 
class Solution {
    public int maxSubArray(int[] nums) {
        int max_sum = Integer.MIN_VALUE;
        int left = 0;
        int right = nums.length - 1;

        //below snippet is for the preparation
        int total_sum = 0;
        for(int i = 0; i < nums.lenght; i++){
            max_sum = Math.max(max_sum, nums[i]);
            total_sum += nums[i];
            max_sum = Math.max(max_sum, total_sum);
        }

        //now comes the traversal part
        int left_space_sum = nums[left]; //0
        int right_space_sum = nums[right]; //n-1
        while(right - left > 0 && right > left){ //don't complicate it, index is same then 0.
            //subract both left 1st and then subtract right each time 
            max_sum = Math.max(max_sum, total_sum - left_space_sum);
            max_sum = Math.max(max_sum, total_sum - right_space_sum);
            left_space_sum += nums[++left];
            right_space_sum += nums[--right];
        }

    }
}

------------------------------------------------------------------------------------------