class Solution {

    public int findInfected(int[][] grid){
        int row = grid.length(-1);
        int column = grid.length();

        for(int i = 0; i < row - 1; i++){
            for(int j = 0; j < column - 1; j++){
                if(grid[i][j] == 2){
                    return new int[]{i, j};
                }
            }
        }
    }

    public int orangesRotting(int[][] grid) {
        int[] infected = findInfected(grid);
        int g_count = 0;
        int count = 0;
        Set<Integer, Integer> visited = new Set<>();

        Queue<Integer, Integer> queue = new Dequeue<>();
        queue.push(infected);
        count++;

        while(!queue.isEmpty() ){
            for(int i = 0; i < count; i++){
                infected = queue.pop();
            }
            //make it infected
            int i = infected[0];
            int j = infected[1];
            grid[i][j] = 2;
            //add the adjacent
            if(i > 0){
                queue.add(new int[]{i-1, j});
                count++;
                queue.add(new int[]{i+1, j});
                count++;
            }
            if(j > 0){
                queue.add(new int[]{i, j - 1});
                count++;
                queue.add(new int[]{i, j + 1});
                count++;
            }
        }
    }
}

-------------------------------------------------------------------------------------------

class Solution {

    int queue_count = 1; //since we have added the rotten orange
    int row_length = 0;
    int col_length = 0;

    public int[] getRottenOrange(int[][] arr){
        row_length = arr.length();
        col_length = arr[0].length();

        for(int i = 0; i < row_length; i++){
            for(int j = 0; j < col_length; j++){
                if(arr[i][j] == 2) return new int[]{i,j};
            }
        }
    }

    public void putVisited(int[] rotten, Map<Integer, Integer>, int count){
        int i = rotten[0];
        int j = rotten[1];

        if(i > 0 && i < row_length - 1){
            queue.push(new int[]{i - 1, j});
            queue.push(new int[]{i + 1, j});
        }

        if(j > 0 && j < col_length - 1){
            queue.push(new int[]{i, j - 1});
            queue.push(new int[]{i , j + 1});
        }

    }

    public int orangesRotting(int[][] grid) {
        Queue<Integer[]> queue = new LinkedList<>();
        int[] rottenOrange = getRottenOrange(grid);

        queue.add(queue);
        int count = 0;
        Map<Integer, Integer> visited = new HashMap<>();
        while(!queue.isEmpty()){
            int temp_count = queue_count;
            queue_count = 0;
            while(temp_count > 0){
                int[] rotten = queue.pop();
                visited.add(rotten);
                putAdjacent(rotten, visited, count);
                temp_count--;
            }
        }


    }
}

----------------------------------------------------------------- (for the question we assumented, there will be only one rotten orange. But it is not)

class Solution {

    int queue_count = 0; //since we have added the rotten orange
    int row_length = 0;
    int col_length = 0;

    public void getRottenOrange(int[][] arr, Queue<Integer[]>){
        row_length = arr.length();
        col_length = arr[0].length();

        for(int i = 0; i < row_length; i++){
            for(int j = 0; j < col_length; j++){
                if(arr[i][j] == 2){
                    queue.push(new int[]{i, j});
                    queue_count++;
                }
            }
        }
    }

    public void putVisited(int[] rotten, Map<Integer, Integer>, int count){
        int i = rotten[0];
        int j = rotten[1];

        if(i > 0 && i < row_length - 1){
            queue.push(new int[]{i - 1, j});
            queue.push(new int[]{i + 1, j});
        }

        if(i = 0){
            queue.push(new int[]{i + 1, j});
        }

        if(i == row_lenght - 1){
            queue.push(new int[]{i - 1, j});
        }

        if(j > 0 && j < col_length - 1){
            queue.push(new int[]{i, j - 1});
            queue.push(new int[]{i , j + 1});
        }


        if(j = 0){
            queue.push(new int[]{i, j + 1});
        }

        if(j == col_length - 1){
            queue.push(new int[]{i, j - 1});
        }
        

    }

    public int orangesRotting(int[][] grid) {
        Queue<Integer[]> queue = new LinkedList<>();
        getRottenOrange(grid, queue);

        int count = 0;
        Map<Integer, Integer> visited = new HashMap<>();
        while(!queue.isEmpty()){
            int temp_count = queue_count;
            queue_count = 0;
            while(temp_count > 0){
                int[] rotten = queue.pop();
                visited.add(rotten);
                putAdjacent(rotten, visited, count);
                temp_count--;
            }
            count++;
        }


    }
}

----------------------------------------------------------------------------

class Solution {

    int queue_count = 0; //since we have added the rotten orange
    int row_length = 0;
    int col_length = 0;

    public void getRottenOrange(int[][] arr, Queue<Integer[]>){
        row_length = arr.length();
        col_length = arr[0].length();

        for(int i = 0; i < row_length; i++){
            for(int j = 0; j < col_length; j++){
                if(arr[i][j] == 2){
                    queue.push(new int[]{i, j});
                    queue_count++;
                }
            }
        }
    }

    public void putVisited(int[] rotten, Map<Integer, Integer> track, int count){
        int i = rotten[0];
        int j = rotten[1];

        if(i > 0 && i < row_length - 1){
            if(!track.containsKey(i - 1)){
                if(track.get(i - 1) != j){
                    queue.push(new int[]{i - 1, j});
                    track.put(i - 1, j);
                }
            }

            if(!track.containsKey(i + 1)){
                if(track.get(i + 1) != j){
                    queue.push(new int[]{i + 1, j});
                    track.put(i + 1, j);
                }
            }
            queue_count += 2;
        }

        if(i = 0){
            if(!track.containsKey(i + 1){
                if(track.get(i + 1) != j){
                    queue.push(new int[]{i + 1, j});
                    track.put(i + 1, j);
                }
            })

            queue_count++;
        }

        if(i == row_lenght - 1){
             if(!track.containsKey(i - 1){
                if(track.get(i - 1) != j){
                    queue.push(new int[]{i - 1, j});
                    track.put(i - 1, j);
                }
            })

            queue_count++;
        }

        if(j > 0 && j < col_length - 1){
            if(!track.containsKey(j + 1){
                if(track.get(j + 1) != i){
                    queue.push(new int[]{i , j + 1});
                }
            
            }

            if(!track.containsKey(j - 1){
                if(track.get(j - 1) != i){
                    queue.push(new int[]{i, j - 1});
                }
            
            }
            queue_count += 2;
        }

        if(j = 0){
            if(!track.containsKey(j + 1){
                if(track.get(j + 1) != i){
                    queue.push(new int[]{i, j + 1});
                }
            
            }
           queue_count++;
        }

        if(j == col_length - 1){
            if(!track.containsKey(j + 1){
                if(track.get(j + 1) != i){
                    queue.push(new int[]{i, j + 1});
                }
            
            }
            queue_count++;
        }
        
    }

    public int orangesRotting(int[][] grid) {
        Queue<Integer[]> queue = new LinkedList<>();
        getRottenOrange(grid, queue);

        int count = 0;
        Map<Integer, Integer> visited = new HashMap<>();
        while (!queue.isEmpty()) {
            int temp_count = queue_count;
            queue_count = 0;
            while (temp_count > 0) {
                int[] rotten = queue.pop();
                visited.add(rotten);
                putAdjacent(rotten, visited, count);
                temp_count--;
            }
            count++;
        }
    }
}


-----------------------------------------------------------------------

class Solution {

    int queue_count = 0; // since we have added the rotten orange
    int row_length = 0;
    int col_length = 0;

    public void getRottenOrange(int[][] arr, Queue<int[]> queue) {
        row_length = arr.length;
        col_length = arr[0].length;

        for (int i = 0; i < row_length; i++) {
            for (int j = 0; j < col_length; j++) {
                if (arr[i][j] == 2) {
                    queue.add(new int[] { i, j });
                    queue_count++;
                }
            }
        }
    }

    public void putVisited(int[] rotten, Map<Integer, Integer> track, int count, Queue<int[]> queue) {
        int i = rotten[0];
        int j = rotten[1];

        if (i > 0 && i < row_length - 1) {
            if (!track.containsKey(i - 1)) { //if this is true, then key is not present
                if (track.get(i - 1) != j) {	//if the key is not present, how will i be getting the value the key which is not even present
                    queue.add(new int[] { i - 1, j });
                    track.put(i - 1, j);
                }
            }

            if (!track.containsKey(i + 1)) {
                if (track.get(i + 1) != j) {
                    queue.add(new int[] { i + 1, j });
                    track.put(i + 1, j);
                }
            }
            queue_count += 2;
        }

        if (i == 0) {
            if (!track.containsKey(i + 1)) {
                if (track.get(i + 1) != j) {
                    queue.add(new int[] { i + 1, j });
                    track.put(i + 1, j);
                }
            }

            queue_count++;
        }

        if (i == row_length - 1) {
            if (!track.containsKey(i - 1)) {
                if (track.get(i - 1) != j) {
                    queue.add(new int[] { i - 1, j });
                    track.put(i - 1, j);
                }
            }

            queue_count++;
        }

        if (j > 0 && j < col_length - 1) {
            if (!track.containsKey(j + 1)) {
                if (track.get(j + 1) != i) {
                    queue.add(new int[] { i, j + 1 });
                }
            }

            if (!track.containsKey(j - 1)) {
                if (track.get(j - 1) != i) {
                    queue.add(new int[] { i, j - 1 });
                }
            }

            queue_count += 2;
        }

        if (j == 0) {
            if (!track.containsKey(j + 1)) {
                if (track.get(j + 1) != i) {
                    queue.add(new int[] { i, j + 1 });
                }
            }
            queue_count++;
        }

        if (j == col_length - 1) {
            if (!track.containsKey(j + 1)) {
                if (track.get(j + 1) != i) {
                    queue.add(new int[] { i, j + 1 });
                }
            }
            queue_count++;
        }
    }

    public int orangesRotting(int[][] grid) {
        Queue<int[]> queue = new LinkedList<>();
        getRottenOrange(grid, queue);

        int count = 0;
        Map<Integer, Integer> visited = new HashMap<>();

        while (!queue.isEmpty()) {
            int temp_count = queue_count;
            queue_count = 0;

            while (temp_count > 0) {
                int[] rotten = queue.poll();

                putVisited(rotten, visited, count, queue);
                temp_count--;
            }

            count++;
        }

        return count;
    }
}

------------------------------------------------------------------------

class Solution {

    int queue_count = 0; // since we have added the rotten orange
    int row_length = 0;
    int col_length = 0;

    public void getRottenOrange(int[][] arr, Queue<int[]> queue) {
        row_length = arr.length;
        col_length = arr[0].length;

        for (int i = 0; i < row_length; i++) {
            for (int j = 0; j < col_length; j++) {
                if (arr[i][j] == 2) {
                    queue.add(new int[] { i, j });
                    queue_count++; //the count here implecitly dictates affected in each stages/time
                }
            }
        }
    }

    public void putVisited(int[] rotten, Map<Integer, Integer> track, int count, Queue<int[]> queue) {
        //getting the vlues stored from the queue
        int i = rotten[0];
        int j = rotten[1];

        if (i > 0 && i < row_length - 1) {
            if (!track.containsKey(i - 1)) {
                    queue.add(new int[] { i - 1, j });
                    track.put(i - 1, j);
            }

            if (!track.containsKey(i + 1)) {
                    queue.add(new int[] { i + 1, j });
                    track.put(i + 1, j);
            }
            queue_count += 2;
        }

        if (i == 0) {
            if (!track.containsKey(i + 1)) {
                    queue.add(new int[] { i + 1, j });
                    track.put(i + 1, j);
            }

            queue_count++;
        }

        if (i == row_length - 1) {
            if (!track.containsKey(i - 1)) {
                    queue.add(new int[] { i - 1, j });
                    track.put(i - 1, j);
            }

            queue_count++;
        }

        if (j > 0 && j < col_length - 1) {
            if (!track.containsKey(j + 1)) {
                    queue.add(new int[] { i, j + 1 });
            }

            if (!track.containsKey(j - 1)) {
                    queue.add(new int[] { i, j - 1 });
                }
            }

            queue_count += 2;
        }

        if (j == 0) {
            if (!track.containsKey(j + 1)) {
                    queue.add(new int[] { i, j + 1 });
            }
            queue_count++;
        }

        if (j == col_length - 1) {
            if (!track.containsKey(j + 1)) {
                    queue.add(new int[] { i, j + 1 });
            }
            queue_count++;
        }
    }

    public int orangesRotting(int[][] grid) {
        Queue<int[]> queue = new LinkedList<>();
        getRottenOrange(grid, queue);

        int count = 0; //counting the stages
        Map<Integer, Integer> visited = new HashMap<>();

        while (!queue.isEmpty()) {
            int temp_count = queue_count;
            queue_count = 0; //note bug : this will get override when just visiting the
            //poping the next element of the same time/stage

            while (temp_count > 0) {
                int[] rotten = queue.poll();

                putVisited(rotten, visited, count, queue);
                temp_count--;
            }

            count++;
        }

        return count;
    }
}

----------------------------------------------------------------------

class Solution {

    int queue_count = 0; // since we have added the rotten orange
    int row_length = 0;
    int col_length = 0;

    public void getRottenOrange(int[][] arr, Queue<int[]> queue) {
        row_length = arr.length;
        col_length = arr[0].length;

        for (int i = 0; i < row_length; i++) {
            for (int j = 0; j < col_length; j++) {
                if (arr[i][j] == 2) {
                    queue.add(new int[] { i, j });
                    queue_count++; //the count here implecitly dictates affected in each stages/time
                }
            }
        }
    }

    public void putVisited(int[] rotten, Map<Integer, Integer> track, int count, Queue<int[]> queue) {
        //getting the vlues stored from the queue
        int i = rotten[0];
        int j = rotten[1];

        if (i > 0 && i < row_length - 1) {
            if (!track.containsKey(i - 1)) {
                    queue.add(new int[] { i - 1, j });
                    track.put(i - 1, j);
            }

            if (!track.containsKey(i + 1)) {
                    queue.add(new int[] { i + 1, j });
                    track.put(i + 1, j);
            }
            queue_count += 2;
        }

        if (i == 0) {
            if (!track.containsKey(i + 1)) {
                    queue.add(new int[] { i + 1, j });
                    track.put(i + 1, j);
            }

            queue_count++;
        }

        if (i == row_length - 1) {
            if (!track.containsKey(i - 1)) {
                    queue.add(new int[] { i - 1, j });
                    track.put(i - 1, j);
            }

            queue_count++;
        }

        if (j > 0 && j < col_length - 1) {
            if (!track.containsKey(j + 1)) {
                    queue.add(new int[] { i, j + 1 });
            }

            if (!track.containsKey(j - 1)) {
                    queue.add(new int[] { i, j - 1 });
                }
            }

            queue_count += 2;
        }

        if (j == 0) {
            if (!track.containsKey(j + 1)) {
                    queue.add(new int[] { i, j + 1 });
            }
            queue_count++;
        }

        if (j == col_length - 1) {
            if (!track.containsKey(j + 1)) {
                    queue.add(new int[] { i, j + 1 });
            }
            queue_count++;
        }
    }

    public int orangesRotting(int[][] grid) {
        Queue<int[]> queue = new LinkedList<>();
        getRottenOrange(grid, queue);

        int count = 0; //counting the stages
        Map<Integer, Integer> visited = new HashMap<>();

        while (!queue.isEmpty()) {
            int temp_count = queue_count;
            queue_count = 0; //note bug : this will get override when just
            //poping the next element of the same time/stage

            while (temp_count > 0) {
                int[] rotten = queue.poll();

                putVisited(rotten, visited, count, queue);
                temp_count--;
            }

            count++;
        }

        return count;
    }
}


--------------------------------------------------------------------------------

class Solution {

    int queue_count = 0; // since we have added the rotten orange
    int row_length = 0;
    int col_length = 0;

    public void getRottenOrange(int[][] arr, Queue<int[]> queue) {
        row_length = arr.length;
        col_length = arr[0].length;

        for (int i = 0; i < row_length; i++) {
            for (int j = 0; j < col_length; j++) {
                if (arr[i][j] == 2) {
                    queue.add(new int[] { i, j });
                    queue_count++; //the count here implecitly dictates affected in each stages/time
                }
            }
        }
    }

    public void putVisited(int[] rotten, Map<Integer, Integer> track, int count, Queue<int[]> queue) {
        //getting the vlues stored from the queue

        /*what dies this method do?
        if keep tracks of visited elements and maintains the elements in particular stage
        for next set of time/iteration/count
        */

        int i = rotten[0];
        int j = rotten[1];

        //condtions to check only valid positions.
        if (i > 0 && i < row_length - 1) {
            //these like conditoin only assessing only one co-ordinate, and missing the another
            if (!track.containsKey(i - 1)) {
                    queue.add(new int[] { i - 1, j });
                    track.put(i - 1, j);
            }

            if (!track.containsKey(i + 1)) {
                    queue.add(new int[] { i + 1, j });
                    track.put(i + 1, j);
            }
            queue_count += 2;
        }

        if (i == 0) {
            if (!track.containsKey(i + 1)) {
                    queue.add(new int[] { i + 1, j });
                    track.put(i + 1, j);
            }

            queue_count++;
        }

        if (i == row_length - 1) {
            if (!track.containsKey(i - 1)) {
                    queue.add(new int[] { i - 1, j });
                    track.put(i - 1, j);
            }

            queue_count++;
        }

        if (j > 0 && j < col_length - 1) {
            if (!track.containsKey(j + 1)) {
                    queue.add(new int[] { i, j + 1 });
            }

            if (!track.containsKey(j - 1)) {
                    queue.add(new int[] { i, j - 1 });
                }
            }

            queue_count += 2;
        }

        if (j == 0) {
            if (!track.containsKey(j + 1)) {
                    queue.add(new int[] { i, j + 1 });
            }
            queue_count++;
        }

        if (j == col_length - 1) {
            if (!track.containsKey(j + 1)) {
                    queue.add(new int[] { i, j + 1 });
            }
            queue_count++;
        }
    }

    public int orangesRotting(int[][] grid) {
        Queue<int[]> queue = new LinkedList<>();
        getRottenOrange(grid, queue);

        int count = 0; //counting the stages
        Map<Integer, Integer> visited = new HashMap<>();

        while (!queue.isEmpty()) {
            int temp_count = queue_count;
            queue_count = 0; //note bug : this will get override when just
            //poping the next element of the same time/stage
            //but wait, that is handled by the loop below

            while (temp_count > 0) {
                int[] rotten = queue.poll();

                putVisited(rotten, visited, count, queue);
                temp_count--;
            }

            count++;
        }

        return count;
    }
}


----------------------------------------------------------------------------------

class Solution {

    int queue_count = 0; // since we have added the rotten orange
    int row_length = 0;
    int col_length = 0;

    public void getRottenOrange(int[][] arr, Queue<int[]> queue) {
        row_length = arr.length;
        col_length = arr[0].length;

        for (int i = 0; i < row_length; i++) {
            for (int j = 0; j < col_length; j++) {
                if (arr[i][j] == 2) {
                    queue.add(new int[] { i, j });
                    queue_count++; //the count here implecitly dictates affected in each stages/time
                }
            }
        }
    }

    public void putVisited(int[] rotten, Map<Integer, Integer> track, int count, Queue<int[]> queue) {
        //getting the vlues stored from the queue

        /*what dies this method do?
        if keep tracks of visited elements and maintains the elements in particular stage
        for next set of time/iteration/count
        */

        int i = rotten[0];
        int j = rotten[1];

        //condtions to check only valid positions.
        if (i > 0 && i < row_length - 1) {
            //these like conditoin only assessing only one co-ordinate, and missing the another

            //let's change the approach
            if(!(track.containsKey(i - 1) && track.getKey(i - 1) == j)){
               queue.add(new int[] { i - 1, j });
                    track.put(i - 1, j);
            }
         
            if(!(track.containsKey(i + 1) && track.getKey(i + 1) == j)){
               queue.add(new int[] { i + 1, j });
                    track.put(i + 1, j);
            }
  
            queue_count += 2;
        }

        if (i == 0) {
                if(!(track.containsKey(i + 1) && track.getKey(i + 1) == j)){
                    queue.add(new int[] { i + 1, j });
                    track.put(i + 1, j);
            }
         
            queue_count++;
        }

        if (i == row_length - 1) {
            if (!(track.containsKey(i - 1) && track.getKey(i - 1) == j)) {
                    queue.add(new int[] { i - 1, j });
                    track.put(i - 1, j);
            }

            queue_count++;
        }

        if (j > 0 && j < col_length - 1) {
            if (!(track.containsKey(j + 1) && track.getKey(j + 1) == i)) {
                    queue.add(new int[] { i, j + 1 });
            }

            if (!(track.containsKey(j - 1) && track.getKey(j - 1) == i)) {
                    queue.add(new int[] { i, j - 1 });
                }
            }

            queue_count += 2;
        }

    if(j==0) {
        if (!(track.containsKey(j + 1) && track.getKey(j + 1) == i)){
            queue.add(new int[] { i, j + 1 });
        }
        queue_count++;
    }

    if(j==col_length-1)
    {
        if (!(track.containsKey(j + 1) && track.getKey(j + 1) == i)) {
            queue.add(new int[] { i, j + 1 });
        }
        queue_count++;
    }
    }

    public int orangesRotting(int[][] grid) {
        Queue<int[]> queue = new LinkedList<>();
        getRottenOrange(grid, queue);

        int count = 0; //counting the stages
        Map<Integer, Integer> visited = new HashMap<>();

        while (!queue.isEmpty()) {
            int temp_count = queue_count;
            queue_count = 0; //note bug : this will get override when just
            //poping the next element of the same time/stage
            //but wait, that is handled by the loop below

            while (temp_count > 0) {
                int[] rotten = queue.poll();

                putVisited(rotten, visited, count, queue);
                temp_count--;
            }

            count++;
        }

        return count;
    }
}

--------------------------------------------------------------------------------------

class Solution {

    int queue_count = 0; // since we have added the rotten orange
    int row_length = 0;
    int col_length = 0;

    public void getRottenOrange(int[][] arr, Queue<int[]> queue) {
        row_length = arr.length;
        col_length = arr[0].length;

        for (int i = 0; i < row_length; i++) {
            for (int j = 0; j < col_length; j++) {
                if (arr[i][j] == 2) {
                    queue.add(new int[] { i, j });
                    queue_count++; //the count here implecitly dictates affected in each stages/time
                }
            }
        }
    }

    public void putVisited(int[] rotten, Map<Integer, Integer> track, int count, Queue<int[]> queue) {
    //getting the vlues stored from the queue

    /*what dies this method do?
    if keep tracks of visited elements and maintains the elements in particular stage
    for next set of time/iteration/count
    */

    int i = rotten[0];
    int j = rotten[1];

    //condtions to check only valid positions.
    if (i > 0 && i < row_length - 1) {
        //these like conditoin only assessing only one co-ordinate, and missing the another

        //let's change the approach
        if (!(track.containsKey(i - 1) && track.get(i - 1) == j)) {
            queue.add(new int[] { i - 1, j });
            track.put(i - 1, j);
        }

        if (!(track.containsKey(i + 1) && track.get(i + 1) == j)) {
            queue.add(new int[] { i + 1, j });
            track.put(i + 1, j);
        }

        queue_count += 2;
    }

    if (i == 0) {
        if (!(track.containsKey(i + 1) && track.get(i + 1) == j)) {
            queue.add(new int[] { i + 1, j });
            track.put(i + 1, j);
        }

        queue_count++;
    }

    if (i == row_length - 1) {
        if (!(track.containsKey(i - 1) && track.get(i - 1) == j)) {
            queue.add(new int[] { i - 1, j });
            track.put(i - 1, j);
        }

        queue_count++;
    }

    if (j > 0 && j < col_length - 1) {
        if (!(track.containsKey(j + 1) && track.get(j + 1) == i)) {
            queue.add(new int[] { i, j + 1 });
        }

        if (!(track.containsKey(j - 1) && track.get(j - 1) == i)) {
            queue.add(new int[] { i, j - 1 });
        }

        queue_count += 2;
    }

    if (j == 0) {
        if (!(track.containsKey(j + 1) && track.get(j + 1) == i)) {
            queue.add(new int[] { i, j + 1 });
        }
        queue_count++;
    }

    if (j == col_length - 1) {
        if (!(track.containsKey(j + 1) && track.get(j + 1) == i)) {
            queue.add(new int[] { i, j + 1 });
        }
        queue_count++;
    }
}


    public int orangesRotting(int[][] grid) {
        Queue<int[]> queue = new LinkedList<>();
        getRottenOrange(grid, queue);

        int count = 0; //counting the stages
        Map<Integer, Integer> visited = new HashMap<>();

        while (!queue.isEmpty()) {
            int temp_count = queue_count;
            queue_count = 0; //note bug : this will get override when just
            //poping the next element of the same time/stage
            //but wait, that is handled by the loop below

            while (temp_count > 0) {
                int[] rotten = queue.poll();

                putVisited(rotten, visited, count, queue);
                temp_count--;
            }

            count++;
        }

        return count;
    }
}

----------------------------------------------------------------------------------

class Solution {

    int queue_count = 0; // since we have added the rotten orange
    int row_length = 0;
    int col_length = 0;

    public void getRottenOrange(int[][] arr, Queue<int[]> queue) {
        row_length = arr.length;
        col_length = arr[0].length;

        for (int i = 0; i < row_length; i++) {
            for (int j = 0; j < col_length; j++) {
                if (arr[i][j] == 2) {
                    queue.add(new int[] { i, j });
                    queue_count++; //the count here implecitly dictates affected in each stages/time
                }
            }
        }
    }

    public void putVisited(int[] rotten, Map<Integer, Integer> track, int count, Queue<int[]> queue) {
    //getting the vlues stored from the queue

    /*what dies this method do?
    if keep tracks of visited elements and maintains the elements in particular stage
    for next set of time/iteration/count
    */

    int i = rotten[0];
    int j = rotten[1];

    //condtions to check only valid positions.
    if (i > 0 && i < row_length - 1) {
        //these like conditoin only assessing only one co-ordinate, and missing the another

        //let's change the approach
        if (!(track.containsKey(i - 1) && track.get(i - 1) == j)) {
            queue.add(new int[] { i - 1, j });
            track.put(i - 1, j);

            queue_count++;
        }

        if (!(track.containsKey(i + 1) && track.get(i + 1) == j)) {
            queue.add(new int[] { i + 1, j });
            track.put(i + 1, j);
            queue_count++;
        }

    }

    if (i == 0) {
        if (!(track.containsKey(i + 1) && track.get(i + 1) == j)) {
            queue.add(new int[] { i + 1, j });
            track.put(i + 1, j);
        }

        queue_count++;
    }

    if (i == row_length - 1) {
        if (!(track.containsKey(i - 1) && track.get(i - 1) == j)) {
            queue.add(new int[] { i - 1, j });
            track.put(i - 1, j);
        }

        queue_count++;
    }

    if (j > 0 && j < col_length - 1) {
        if (!(track.containsKey(j + 1) && track.get(j + 1) == i)) {
            queue.add(new int[] { i, j + 1 });
            queue_count++;
        }

        if (!(track.containsKey(j - 1) && track.get(j - 1) == i)) {
            queue.add(new int[] { i, j - 1 });
            queue_count++;
        }

    }

    if (j == 0) {
        if (!(track.containsKey(j + 1) && track.get(j + 1) == i)) {
            queue.add(new int[] { i, j + 1 });
        }
        queue_count++;
    }

    if (j == col_length - 1) {
        if (!(track.containsKey(j + 1) && track.get(j + 1) == i)) {
            queue.add(new int[] { i, j + 1 });
        }
        queue_count++;
    }
}


    public int orangesRotting(int[][] grid) {
        Queue<int[]> queue = new LinkedList<>();
        getRottenOrange(grid, queue);

        int count = 0; //counting the stages
        Map<Integer, Integer> visited = new HashMap<>();

        while (!queue.isEmpty()) {
            int temp_count = queue_count;
            queue_count = 0; //note bug : this will get override when just
            //poping the next element of the same time/stage
            //but wait, that is handled by the loop below
            

            /*After time exceeding error, i am wonder where is this reason causing the issue
             when the queue is not empty and and temp by any chance temp_count is less than 0. Yup that is the reason, i tested using a snipped in the below
            */
            while (temp_count > 0) {
                int[] rotten = queue.poll();

                putVisited(rotten, visited, count, queue);
                temp_count--;
            }

            //if(temp_count <= 0) return count; //only for testing

            count++;
        }

        return count;
    }
}


------------------------------------------------------------------------------------

class Solution {

    int queue_count = 0; // since we have added the rotten orange
    int row_length = 0;
    int col_length = 0;

    public void getRottenOrange(int[][] arr, Queue<int[]> queue) {
        row_length = arr.length;
        col_length = arr[0].length;

        for (int i = 0; i < row_length; i++) {
            for (int j = 0; j < col_length; j++) {
                if (arr[i][j] == 2) {
                    queue.add(new int[] { i, j });
                    queue_count++; //the count here implecitly dictates affected in each stages/time
                }
            }
        }
    }

    public void putVisited(int[] rotten, Map<Integer, Integer> track, int count, Queue<int[]> queue) {
    //getting the vlues stored from the queue

    /*what dies this method do?
    if keep tracks of visited elements and maintains the elements in particular stage
    for next set of time/iteration/count
    */

    int i = rotten[0];
    int j = rotten[1];

    //condtions to check only valid positions.
    if (i > 0 && i < row_length - 1) {
        //these like conditoin only assessing only one co-ordinate, and missing the another

        //let's change the approach
        if (!(track.containsKey(i - 1) && track.get(i - 1) == j)) {
            queue.add(new int[] { i - 1, j });
            track.put(i - 1, j);
            queue_count++;
        }

        if (!(track.containsKey(i + 1) && track.get(i + 1) == j)) {
            queue.add(new int[] { i + 1, j });
            track.put(i + 1, j);
            queue_count++;
        }

    }

    if (i == 0) {
        if (!(track.containsKey(i + 1) && track.get(i + 1) == j)) {
            queue.add(new int[] { i + 1, j });
            track.put(i + 1, j);
            queue_count++;
        }
        /* putting inside the if statement
        queue_count++;
        */
    }

    if (i == row_length - 1) {
        if (!(track.containsKey(i - 1) && track.get(i - 1) == j)) {
            queue.add(new int[] { i - 1, j });
            track.put(i - 1, j);
            queue_count++;
        }
    }

    if (j > 0 && j < col_length - 1) {
        //had issue here (track.containsKey(j + 1) && track.get(j + 1) == i)
        if (!(track.containsKey(i) && track.get(i) == j + 1)) {
            queue.add(new int[] { i, j + 1 });
            queue_count++;
        }

        //had issue here (track.containsKey(j - 1) && track.get(j - 1) == i)
        if (!(track.containsKey(i) && track.get(i) == j - 1)) {
            queue.add(new int[] { i, j - 1 });
            queue_count++;
        }

    }

    if (j == 0) {
        //had issue here track.containsKey(j + 1) && track.get(j + 1) == i)
        if (!(track.containsKey(i) && track.get(i) == j + 1)) { 
            queue.add(new int[] { i, j + 1 });
            queue_count++;
        }
        /* putting inside the if statement
        queue_count++;
        */
    }

    if (j == col_length - 1) {
        //had issue here (track.containsKey(j + 1) && track.get(j + 1) == i)
        if (!(track.containsKey(i) && track.get(i) == j + 1)) {
            queue.add(new int[] { i, j + 1 });
            queue_count++;
        }
        /* putting inside the if statement
        queue_count++;
        */
    }
}


    public int orangesRotting(int[][] grid) {
        Queue<int[]> queue = new LinkedList<>();
        getRottenOrange(grid, queue);

        int count = 0; //counting the stages
        Map<Integer, Integer> visited = new HashMap<>();

        while (!queue.isEmpty()) {
            int temp_count = queue_count;
            queue_count = 0; //note bug : this will get override when just
            //poping the next element of the same time/stage
            //but wait, that is handled by the loop below
            

            /*After time exceeding error, i am wonder where is this reason causing the issue
             when the queue is not empty and and temp by any chance temp_count is less than 0. Yup that is the reason, i tested using a snipped in the below
            */
            while (temp_count > 0) {
                int[] rotten = queue.poll();

                putVisited(rotten, visited, count, queue);
                temp_count--;
            }

            //if(temp_count <= 0) return count; //only for testing

            count++;
        }

        return count;
    }
}


----------------------------------------------------------------------- (This isn't working. There are causing boundey issue For eg : grid = [[0,2]]

import java.util.*;

class Solution {

    int queue_count = 0;
    int row_length = 0;
    int col_length = 0;

    public void getRottenOrange(int[][] arr, Queue<int[]> queue, Set<String> visited) {
        row_length = arr.length;
        col_length = arr[0].length;
        
        for (int i = 0; i < row_length; i++) {
            for (int j = 0; j < col_length; j++) {
                if (arr[i][j] == 2) {
                    queue.add(new int[] { i, j });
                    visited.add(i + "," + j);
                    queue_count++;
                }
            }
        }
    }

    public void putVisited(int[] rotten, Set<String> track, int count, Queue<int[]> queue) {

        int i = rotten[0];
        int j = rotten[1];

        if (i > 0 && i < row_length - 1) {

            if (!track.contains((i - 1) + "," + j)) {
                queue.add(new int[] { i - 1, j });
                track.add((i - 1) + "," + j);
                queue_count++;
            }

            if (!track.contains((i + 1) + "," + j)) {
                queue.add(new int[] { i + 1, j });
                track.add((i + 1) + "," + j);
                queue_count++;
            }
        }

        if (i == 0) {
            if (!track.contains((i + 1) + "," + j)) {
                queue.add(new int[] { i + 1, j });
                track.add((i + 1) + "," + j);
                queue_count++;
            }
        }

        if (i == row_length - 1) {
            if (!track.contains((i - 1) + "," + j)) {
                queue.add(new int[] { i - 1, j });
                track.add((i - 1) + "," + j);
                queue_count++;
            }
        }

        if (j > 0 && j < col_length - 1) {

            if (!track.contains(i + "," + (j + 1))) {
                queue.add(new int[] { i, j + 1 });
                track.add(i + "," + (j + 1));
                queue_count++;
            }

            if (!track.contains(i + "," + (j - 1))) {
                queue.add(new int[] { i, j - 1 });
                track.add(i + "," + (j - 1));
                queue_count++;
            }
        }

        if (j == 0) {
            if (!track.contains(i + "," + (j + 1))) {
                queue.add(new int[] { i, j + 1 });
                track.add(i + "," + (j + 1));
                queue_count++;
            }
        }

        if (j == col_length - 1) {
            if (!track.contains(i + "," + (j - 1))) {
                queue.add(new int[] { i, j - 1 });
                track.add(i + "," + (j - 1));
                queue_count++;
            }
        }
    }

    public int orangesRotting(int[][] grid) {
        Queue<int[]> queue = new LinkedList<>();
        Set<String> visited = new HashSet<>();

        getRottenOrange(grid, queue, visited);

        int count = 0;

        while (!queue.isEmpty()) {

            int temp_count = queue_count;
            queue_count = 0;

            while (temp_count > 0) {
                int[] rotten = queue.poll();

                putVisited(rotten, visited, count, queue);
                temp_count--;
            }

            count++;
        }

        return count;
    }
}

-----------------------------------------------------------

import java.util.*;

class Solution {

    int queue_count = 0;
    int row_length = 0;
    int col_length = 0;
    int[][] visited = null; //if i don't know the size of the array

    public void getRottenOrange(int[][] arr, Queue<int[]> queue) {
        row_length = arr.length;
        col_length = arr[0].length;
        
        for (int i = 0; i < row_length; i++) {
            for (int j = 0; j < col_length; j++) {
                if (arr[i][j] == 2) {
                    queue.add(new int[] { i, j });
                    visited[i][j] = 2;
                    queue_count++;
                }
            }
        }
    }

    public void putVisited(int[] rotten, int[][] track, int count, Queue<int[]> queue) {

        int i = rotten[0];
        int j = rotten[1];

        /*
        1. Address boundary Condition
        2. Check for Visited, mark visited, put it in next chain/stage/count

        i shouldn't reach -1, row_length
        j shouldn't reach -1, col_length
        
        then we need conditions to present it from reaching/checking
        -1 for i would come where looking for west element
        row_lenght would come when looking for ease element

        -1 for j would come when looking for south element
        col_length would come when looking for north element

        when we are in 1st row(0), we shouldn't add/check anything above
        when we are in last row(row_length - 1), we shouldn't add/check anything below

        the above discussion is not upto the mark!

        just don't check the negative and out of boundplaces
        */

        //Simple logic below:
        1. for each elemenet i ll be adding the adjacent right, so
            i + 1, i - 1, j + 1, and j - 1
            i + 1 i - 1, row changes but not the columen, looking up and down
            j + 1, j - 1, column changes but not the rowm looking left and right

            if i and j both are zero, check only i + 1 (down) and j + 1 (right)
            if i and j both are maxed, check onlu i - 1 (up) and j - 1 (left)

            if i is zero, j is maxed, check j - 1, j + 1, i + 1;
            if i is not zero, j


        


    }

    public int orangesRotting(int[][] grid) {
        Queue<int[]> queue = new LinkedList<>();

        row_length = grid.length();
        col_length = grid[0].length();
        visited = new int[row_length][col_length];

        getRottenOrange(grid, queue);

        int count = 0;

        while (!queue.isEmpty()) {

            int temp_count = queue_count;
            queue_count = 0;

            while (temp_count > 0) {
                int[] rotten = queue.poll();

                putVisited(rotten, visited, count, queue);
                temp_count--;
            }

            count++;
        }

        return count;
    }
}


------------------------------------------------------------- //not sure yet, where am i getting wrong, since i not getting count = -1 when desired
it only signal it is not affecting the oranges as expected

import java.util.*;

class Solution {

    int queue_count = 0;
    int row_length = 0;
    int col_length = 0;
    int healthy = 0;
    int[][] visited = null; //if i don't know the size of the array

    public void getRottenOrange(int[][] arr, Queue<int[]> queue) {
        
        for (int i = 0; i < row_length; i++) {
            for (int j = 0; j < col_length; j++) {
                if (arr[i][j] == 2) {
                    queue.add(new int[] { i, j });
                    visited[i][j] = 2;
                    queue_count++; // i don't have to increment the count ie count++
                }
                else healthy++;
            }
        }
    }

    public void putVisited(int[] rotten, int[][] track, Queue<int[]> queue) {

        int i = rotten[0];
        int j = rotten[1];

        if(i - 1 >= 0){
            if(track[i - 1][j] == 0){
                track[i - 1][j] = 2;
                queue.add(new int[]{i - 1, j});
                healthy--;
                queue_count++;
            }
        }

        if(i + 1 < row_length){
            if(track[i + 1][j] == 0){
                track[i + 1][j] = 2;
                queue.add(new int[]{i + 1, j});
                healthy--;
                queue_count++;
            }
        }

        if(j - 1 >= 0){
            if(track[i][j - 1] == 0){
                track[i][j - 1] = 2;
                queue.add(new int[]{i, j - 1});
                healthy--;
                queue_count++;
            }
        }

        if(j + 1 < col_length){
            if(track[i][j + 1] == 0){
                track[i][j + 1] = 2;
                queue.add(new int[]{i, j + 1});
                healthy--;
                queue_count++;
            }
        }
    }

    public int orangesRotting(int[][] grid) {
        Queue<int[]> queue = new LinkedList<>();

        row_length = grid.length;
        col_length = grid[0].length;
        visited = new int[row_length][col_length];

        getRottenOrange(grid, queue);

        int count = -1;

        while (!queue.isEmpty()) {

            int temp_count = queue_count;
            queue_count = 0;

            while (temp_count > 0) {
                int[] rotten = queue.poll();
                putVisited(rotten, visited, queue);
                temp_count--;
            }

            count++;
        }

        if(healthy != 0) { //there are some oranges left unaffected
            return -1;
        }
        return count;
    }
}

------------------------------------------------------------------------- //305 / 307 testcases passed

import java.util.*;

class Solution {

    int queue_count = 0;
    int row_length = 0;
    int col_length = 0;
    int healthy = 0;
    int[][] visited = null; //if i don't know the size of the array\
    int[][] grid = null;

    public void getRottenOrange(int[][] arr, Queue<int[]> queue) {
        
        for (int i = 0; i < row_length; i++) {
            for (int j = 0; j < col_length; j++) {
                if (arr[i][j] == 2) {
                    queue.add(new int[] { i, j });
                    visited[i][j] = 2;
                    queue_count++; // i don't have to increment the count ie count++
                }
                if(arr[i][j] == 1){ //mistake : i didn't consider the empty cell
                    healthy++;
                }
            }
        }
    }

    public void putVisited(int[] rotten, int[][] track, Queue<int[]> queue) {

        int i = rotten[0];
        int j = rotten[1];

        if(i - 1 >= 0){
            if((track[i - 1][j] == 0) && (grid[i - 1][j] == 1)){ //didn't consider where orange it present or not
                track[i - 1][j] = 2;
                queue.add(new int[]{i - 1, j});
                healthy--;
                queue_count++;
            }
        }

        if(i + 1 < row_length){
            if((track[i + 1][j] == 0)  && (grid[i + 1][j] == 1)){
                track[i + 1][j] = 2;
                queue.add(new int[]{i + 1, j});
                healthy--;
                queue_count++;
            }
        }

        if(j - 1 >= 0){
            if((track[i][j - 1] == 0) && (grid[i][j - 1] == 1)){
                track[i][j - 1] = 2;
                queue.add(new int[]{i, j - 1});
                healthy--;
                queue_count++;
            }
        }

        if(j + 1 < col_length){
            if((track[i][j + 1] == 0) && (grid[i][j + 1] == 1)){
                track[i][j + 1] = 2;
                queue.add(new int[]{i, j + 1});
                healthy--;
                queue_count++;
            }
        }
    }

    public int orangesRotting(int[][] grid) {
        Queue<int[]> queue = new LinkedList<>();
        this.grid = grid;

        row_length = grid.length;
        col_length = grid[0].length;
        visited = new int[row_length][col_length];


        getRottenOrange(grid, queue);

        int count = -1;

        while (!queue.isEmpty()) {

            int temp_count = queue_count;
            queue_count = 0;

            while (temp_count > 0) {
                int[] rotten = queue.poll();
                putVisited(rotten, visited, queue);
                temp_count--;
            }

            count++;
        }

        if(healthy != 0) { //there are some oranges left unaffected
            return -1;
        }
        return count;
    }
}